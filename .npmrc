# package.json 의 engines(node >=22) 를 경고가 아니라 설치 실패로 강제한다.
# 이게 없으면 .nvmrc·engines·CI 가 모두 22 를 가리켜도 v20 셸에서 install 이 그냥 통과해,
# 선언과 다른 런타임으로 서비스가 떠도 아무도 모른다. (2026-08-13 실제로 v20 으로 떠 있었음)
engine-strict=true
