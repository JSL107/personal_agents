# 남은 일 재고 — 2026-09-08 (같은 날 2차 대조)

> **한 줄로**: 같은 날 아침에 쓴 [1차 재고](./2026-09-08-remaining-work-inventory.md)의 §3·§4 를
> 다시 코드와 대조했다. **§3-1 은 이미 해소돼 있었고, §3-2 는 고쳤다(#496).** 취약점은 운영 7건이
> 그대로인데 **개발 쪽에 21건이 새로 떠서** 그것을 걷어냈다(#497). 원격 브랜치는 조사만 하고
> **사용자 판단으로 삭제하지 않기로 했다.**

1차 문서 §5 가 "새 결정은 이 문서를 고치지 말고 새 파일로 남긴다" 고 정한 규칙에 따라 새로 쓴다.
판정은 문서가 아니라 코드·PR·실측을 근거로 삼는 3단계 절차를 그대로 썼다.

---

## 1. 1차 문서 이후 달라진 것

| 1차 항목 | 지금 | 근거 |
|---|---|---|
| **§3-1** 최소 창에서 상태 바가 상단 밴드 문패를 덮는다 | **해소 — 재현 안 됨** | 아래 §1-1 |
| **§3-2** `state.changed` 가 말풍선 문구를 안 싣는다 | **고침 — PR #496 머지** | 아래 §1-2 |
| **§3-3** 취약점 7건(운영) | **운영 7건 그대로 · 개발 21건 신규 → 걷어냄(PR #497 머지)** | 아래 §1-3 |
| **§4** 원격 브랜치 262개 | **266개. 조사 완료, 삭제 안 함(사용자 판단)** | 아래 §1-4 |

### 1-1. 최소 창 상태 바 겹침 — #492 가 이미 없앴다

1차 문서는 `--render --size 640x560` 에서 좌상단 상태 바가 회의실 문패·대표 이름표 위에
겹친다고 적었다. **지금 다시 굽자 겹치지 않는다.**

이유는 창 최소 크기가 바뀌었기 때문이다. `Theme.swift:99-101` 의 `windowMinWidth` 는 **720**,
`windowMinHeight` 는 560 이다 — 1차 문서가 쓴 640 은 앱이 허용하지 않는 폭이다. 실제 최소인
720×560 과, 문서가 쓴 640×560 둘 다 굽어 확인했다.

| 크기 | 상태 바가 덮는 것 |
|---|---|
| 720×560 (실제 최소) | 도면 좌상단 그림(창문·사물함)만. 문패·이름표와 안 겹침 |
| 640×560 (문서 기준) | 같음 |
| 900×600 | 검은 여백 위. 도면과도 안 겹침 |

**남는 것은 결함이라 부르기 어렵다.** 최소 창 구간에서는 도면이 화면을 꽉 채우므로 상태 바
같은 겉면 표시가 도면 위에 얹히는 것은 피할 수 없다. 글자를 가리지는 않는다.

**교훈**: 스냅샷의 재현 조건(창 크기)이 그 사이 코드에서 바뀌면, 같은 인자로 다시 굽어도
같은 화면이 아니다. 재현 전에 그 값이 지금 코드에서 얼마인지 먼저 읽을 것.

### 1-2. `state.changed` 말풍선 — PR #496

`console.type.ts` 의 `state.changed` payload 에 `bubble` 을 추가하고, 문구 조립을
`bubbleForActiveRun()` 한 곳으로 모아 스냅샷 경로와 이벤트 경로가 같은 값을 내게 했다.

**시도했다가 되돌린 것이 하나 있다 — 이게 이 항목에서 가장 값진 기록이다.**

문구를 이벤트가 싣게 됐으니 화면 쪽 우회(상태 변경마다 스냅샷 재조회)를 지워도 될 것처럼
보였다. 실제로 완료·구버전만 남기고 좁혔는데, 외부 리뷰가 회귀 두 건을 지적했고 코드에서
확인하니 둘 다 맞았다.

1. **이벤트는 런 한 건, 화면은 그 사람 전체의 집계다.** `deriveAgentState` 는 열린 승인 >
   활성 런 > 마지막 종료 순으로 하나를 고른다. 승인이 열린 채 새 런이 시작되면 이벤트는
   `IN_PROGRESS`, 집계는 `AWAITING_APPROVAL` 이다. 재조회가 그 어긋남을 2초 안에 정정하고
   있었고, 좁히면 30초 남는다.
2. **`resyncSnapshot()` 끝에 `resyncBriefing()` 이 붙어 있다.** 재조회를 건너뛰면 대표
   브리핑의 실패 건수·할 일 갱신까지 함께 건너뛴다.

**교훈**: 우회를 걷어낼 때는 "그 우회가 무엇을 고쳐 주고 있었나" 를 전수로 세어야 한다.
주석에 적힌 이유(여기서는 말풍선)가 그 코드가 하는 일의 전부가 아니다. 특히 `resyncX()`
류처럼 **다른 갱신을 꼬리에 달고 있는 함수**는 이름만 보고 판단하면 안 된다.

**이벤트↔집계 불일치는 절반만 막았다.** PR 리뷰(codex · 이대리 봇 둘 다 같은 곳을 짚었다)를
받아 클라이언트 스토어에 가드를 넣었다 — 상태가 종료로 바뀌는데 그 사람에게 안 끝난 런이
남아 있으면 적용하지 않는다.

**승인이 열린 경우는 일부러 안 막았다. 이유가 대칭이 아니다.**

- 종료를 막으면 남은 런이 끝날 때 **또 다른 종료 이벤트가 와서** 저절로 회복된다.
- 승인이 열린 동안 온 `IN_PROGRESS` 를 막으면, 그 런은 이미 시작했으므로 **다시 보내 주는
  이벤트가 없다.** 승인이 닫혀도 상태가 안 돌아오고 30초 주기 스냅샷까지 기다려야 한다 —
  지금보다 나빠진다.

**교훈**: 이벤트를 드롭하는 가드는 "그 이벤트가 다시 올 기회가 있나" 를 먼저 봐야 한다.
재발행되는 이벤트(종료)는 막아도 되고, 한 번뿐인 이벤트(시작)는 막으면 그 자리에 구멍이 난다.

### 1-3. 취약점 — 운영 7건 그대로, 개발 21건 신규

1차 문서가 남긴 7건은 **운영(`--prod`) 기준이었다.** 그런데 그 뒤 개발 의존성 쪽에 새 권고가
21건 떠서 전체는 28건이 됐다. **1차 문서가 기준을 「권고 vs 경로」로만 갈랐는데, 실제로는
「운영 vs 전체」 축이 하나 더 있다.**

| 기준 | 1차(09-08 오전) | 2차(09-08 오후) | PR #497 이후 |
|---|---|---|---|
| 전체 · 경로 | 7 | **28** | **7** |
| 전체 · 권고 | 7 | **28** | **7** |
| 운영(`--prod`) · 경로 | 7 | 7 | 7 |

**인용할 때 축 두 개를 함께 적는다** — 「운영/전체」와 「권고/경로」. 셋 다 다른 값이 나올 수
있다(1차에서는 51 vs 55 였다).

PR #497 은 #493 이 세운 규칙(**키에 대상 major 를 붙인다**)을 그대로 따라 21건을 없앴다. 그
규칙이 실제로 일한 자리가 셋이다.

| 패키지 | 함께 깔려 있던 것 | 한정 키가 막은 것 |
|---|---|---|
| `ajv` | 6.14.0 · 8.12.0 | 권고는 7 이상만 해당. `ajv@8` 로 6.14.0 보존 |
| `glob` · `picomatch` | 7.2.3 / 2.3.2 가 대상 아님 | `glob@10` · `picomatch@4` 로 한정 |
| `brace-expansion` | **1·2·5 세 라인 동시 설치** | 라인별 키 3개. 한 키로 묶으면 나머지 부모가 깨진다 |

**`tmp` 하나만 부모가 선언한 범위를 넘는다.** `external-editor@3.1.0` 이 `tmp ^0.0.33` 을
선언했는데 0.0.x 에 수정본이 없다. `tmp@0.0` 키로 0.0 라인만 0.2.7 로 올렸고, 실제 쓰는
API 를 변경 전후로 나란히 돌려 결과가 같음을 확인했다(임시파일 생성·읽기·정리 전부 동일).
**범위를 넘길 때는 게이트가 아니라 그 소비자의 실제 호출을 대조해야 한다** — 이 경로는
대화형 프롬프트라 테스트가 지나가지 않는다.

**남은 7건은 1차 문서 §3-3 의 표 그대로다** — 네 경로(NestJS 10→11 · `@huggingface/transformers` ·
puppeteer 24→25 · Prisma 6→7)가 서로 독립이라 넷을 다 올려야 없어진다. `CLAUDE.md` §0 이
NestJS 10 · Prisma 6 을 스택 사실로 박아 뒀으므로 그 문서도 함께 고쳐야 한다.

### 1-4. 원격 브랜치 — 조사는 끝냈고, 삭제는 안 한다

**사용자 판단으로 원격 브랜치는 지우지 않기로 했다.** 조사 결과만 기록으로 남긴다.

원격 heads **266개**(main 포함). `git branch -r --merged origin/main` 은 82개만 잡는데,
**이 레포는 squash 머지라 머지된 브랜치도 조상 관계가 끊긴다** — 이 명령은 판정 근거로 못 쓴다.

브랜치마다 `gh pr list --head <branch> --state all` 로 PR 상태를 조회해 이렇게 갈렸다.

| 분류 | 개수 |
|---|---|
| PR 머지 완료 + 머지 후 push 없음 | **249** |
| PR 머지 완료지만 **머지 시각 뒤에 커밋이 있음** | 8 |
| PR 이 아예 없음 | 2 |
| PR 이 닫혔고 미머지 | 3 |
| 진행 중(OPEN·작업 중) | 4 |

**두 번째 줄이 이 조사의 핵심이다.** PR 상태만 보고 지우면 머지 뒤에 얹은 커밋이 함께
사라진다(이 레포에서 실제로 세 번 재발한 유형이다). 그래서 **브랜치 tip 의 커밋 시각과 PR
`mergedAt` 을 대조**해 뒤에 붙은 것을 따로 갈랐다. 해당 8개는 이렇다.

`feat/autopilot-knowledge-lint` · `feat/episodic-memory` · `feat/office-visual-pass` ·
`feat/v3-router-manager-pattern` · `feat/vacation-calculator` ·
`fix/conversational-reply-di-and-drop-gemini` · `fix/episodic-docs-sync` ·
`fix/intent-classifier-bot-state-question`

**여기서 막힌 것 하나** — 그 8개의 머지 후 커밋이 실제로 main 에 반영됐는지는 **판정하지
못했다.** 시도한 방법과 왜 안 되는지:

- `git log main..branch` — squash 라 브랜치 커밋이 전부 "미반영" 으로 나온다. 무용.
- `git cherry` — squash 가 patch-id 를 바꾼다. 무용.
- `git diff main branch -- <해당 파일>` — main 이 그 뒤 3개월 치 움직여서, 차이가 나도
  그게 미반영 때문인지 이후 변경 때문인지 갈라지지 않는다(실제로 수천 줄 차이가 나왔다).

**다음에 판정하려면** 커밋 하나씩 그 내용이 지금 main 에 있는지 직접 읽는 수밖에 없다
(8개 × 1~4 커밋이라 감당 가능한 양이다).

**로컬 부산물 하나** — `refs/remotes/origin` 이라는 이름 없는 stale ref 가 하나 있다.
`git branch -r` 이 이것 때문에 원격보다 1개 많게 센다(267 vs 266). 원격에는 없는 로컬
찌꺼기라 지워도 원격에 영향이 없다.

---

## 2. 이번에도 확인 못 한 것

1차 문서 §2 의 5·6 번이 그대로다. 둘 다 정지 화면으로는 판정할 수 없다.

| # | 항목 | 왜 아직인지 |
|---|---|---|
| 5 | 복도를 걷는 사람이 실제로 보이는지 | 셔츠 밝기 186 vs 복도 157 의 차이 29 가 충분한지. **걷는 모습**을 봐야 한다 |
| 6 | 8초 주기 배회와 마우스 호버 쪽지 모양 | 실앱에서 사람이 봐야 한다 |

여기에 이번 것 하나가 붙는다.

| # | 항목 | 왜 아직인지 |
|---|---|---|
| 7 | 말풍선이 실앱에서 실제로 더 빨리 뜨는지 | 이대리 워커가 도는 타이밍을 잡아야 한다. PR #496 은 유닛 단언까지만 했다 |

---

## 3. 진짜 남은 것

### 3-1. 슬래시 블로그 발행 카드의 30분 만료 (1차 §3-2 에서 이관, 그대로)

슬래시로 만든 블로그 발행 카드는 `responseUrl` 을 들고 24시간 TTL 을 쓴다
(`publish-notion-draft.usecase.ts:88·241`). Slack `response_url` 은 30분 만료라, 30분이 지나
누르면 카드 상태는 DB 에 반영되지만 슬랙 메시지 갱신만 조용히 실패한다. 저녁 자동 경로처럼
채널 좌표(`channelId`/`messageTs`)를 함께 저장하면 해소된다.

### 3-2. 이벤트 상태와 집계 상태가 갈린다 — 절반 남음 (신규 · 잠복)

`state.changed` 는 런 한 건의 상태를, 스냅샷은 그 사람 전체의 집계(`deriveAgentState` —
열린 승인 > 활성 런 > 마지막 종료)를 낸다.

PR #496 이 **겹친 런** 쪽은 클라이언트 가드로 막았다. **승인이 열린 채 새 런이 시작되는 쪽은
그대로다** — 막으면 오히려 나빠져서 안 막았다(§1-2 의 교훈).

근본 해소는 발행 시점에 집계를 계산하는 것이고, 그러려면 `AgentRunService.execute` 가 매
발행마다 열린 승인과 그 agentType 의 다른 활성 런을 조회해야 한다. 런 하나의 라이프사이클만
아는 자리라 그 조회를 붙이는 것이 이 항목의 실제 비용이다.

### 3-3. 취약점 7건 — 네 경로의 major 업그레이드

1차 문서 §3-3 의 표 그대로다. 경로 하나씩 별도 PR 이 안전하다.

| 경로 | 필요한 업그레이드 | 빠지는 건수 |
|---|---|---|
| NestJS | 10 → **11**(하한 11.1.18, 12 아님) | 3 |
| `@huggingface/transformers` | 4.2.0 → 상위 | 2 |
| puppeteer | 24 → 25 | 1 |
| Prisma | 6 → 7 | 1 |

---

## 4. 이 문서를 어떻게 쓰나

1차 문서 §5 를 그대로 잇는다. 덧붙일 것 셋:

- **재현 조건이 코드 상수면 먼저 읽어라.** 스냅샷에 적힌 창 크기·임계값이 그 사이 바뀌면
  같은 인자로 굽어도 같은 화면이 아니다(§1-1 의 720 vs 640).
- **우회를 걷어낼 때는 그 우회가 고쳐 주던 것을 전수로 세어라.** 주석에 적힌 이유가 전부가
  아니고, 꼬리에 다른 갱신을 달고 있는 함수가 있다(§1-2).
- **취약점 건수는 축이 셋이다** — 운영/전체 · 권고/경로. 셋 다 적어야 재현된다(§1-3).
- **이 문서를 새로 만들 때는 `git add -f` 가 필요하다.** `.gitignore:78` 에 `docs/` 가 있어서
  `docs/superpowers/plans/` 아래 새 파일은 `git add -A` 로 안 잡힌다. 기존 plan 파일들은 전부
  force-add 로 들어와 있다. `git status` 가 조용해서 커밋한 줄 알고 넘어가기 쉽다 — 실제로
  이 문서를 쓸 때 한 번 걸렸다.
