# 남은 일 재고 — 2026-08-13 전수 대조

> **한 줄로** — 계획 문서의 체크박스는 완료 신호가 아니다. 문서의 주제를 코드와 하나씩 대조한
> 결과, **계획해 놓고 안 만든 기능은 없었다.** 남은 것은 "확인만 하면 되는 것"과 "치울 것"이다.

기준 커밋 `13c4bb5`

| 대상 | git 추적 | 로컬 파일 |
|---|---|---|
| `docs/superpowers/plans/` | 49 | 74 |
| `docs/superpowers/specs/` | 39 | 60 |
| `docs/superpowers/audits/` | 2 | 8 |
| 합계 | **90** | **142** |

**`docs/` 는 gitignore 다.** 이미 추적 중인 파일은 따라오지만 새로 만든 문서는 `git add -f`
없이는 커밋되지 않는다. 그래서 내 로컬에는 142개가 있어도 **다른 클론에서는 90개만 보인다.**
아래 대조는 로컬 142개를 대상으로 했고, 추적되지 않는 52개의 판정은 이 문서를 읽는 쪽에서
재현할 수 없다 — 그 한계를 안고 읽어야 한다.

---

## 왜 이 문서가 필요한가

계획 문서는 **그날의 기록**이고 사후 갱신하지 않는 것이 이 레포의 규칙이다([CLAUDE.md](../../../CLAUDE.md) §7).
그래서 각 문서 말미의 "남은 것" 표와 본문의 체크박스는 **작성 시점의 계획**이지 현재 잔여가 아니다.

이 차이가 실제로 문제를 일으켰다. 잔여를 묻는 요청에 문서의 체크박스를 세어 답했더니 절반이
틀렸다. 가장 확실한 반례는 이것이다.

| 문서 | 체크박스 | 실제 |
|---|---|---|
| `2026-08-11-paper-trading-phase1.md` | 미완 55 / 전체 55 | **#273 으로 머지 완료, 운영 중** |
| `2026-04-28-p2-features.md` | 미완 79 / 전체 85 | `/retry-run` 구현·운영 중 |

**이 레포는 일을 끝내고 체크박스를 채우지 않는다.** 체크박스로 잔여를 판정하면 안 된다.

---

## 어떻게 판정했나 (재현 가능하게)

문서 대신 **코드·PR·실행 기록**을 근거로 삼았다.

1. **기능 존재** — 문서의 주제어로 `src/` 에 디렉터리·파일이 있는지. **못 찾은 것과 없는 것은
   다르다.** 한 번의 grep 이 비었다고 미구현으로 적으면 다음 사람이 이미 있는 기능을 다시 만든다.
   이 문서의 초안이 실제로 그 실수를 세 번 했다 — `Theme.swift` 를 `Sources/ConsoleCore/` 에서
   찾다 "없음" 으로 오판(진짜 위치는 `Sources/IdaeriConsole/`), 산출물이 `src/` 밖(`~/.claude/`)
   인 문서 둘을 미구현으로 분류, v3 감사 P2 세 건을 "코드에서 못 찾음" 으로 잔여에 등재.
   **찾지 못했으면 "미구현" 이 아니라 "미확인" 으로 적고, 확인 방법을 함께 남긴다.**
2. **브랜치 머지 여부** — `gh pr list --state all --json headRefName,state`. **커밋 수로 판정하지
   말 것**: squash 머지라 `git rev-list --count origin/main..<branch>` 는 머지된 브랜치에서도
   0 이 아니다.
3. **워커 실행 여부** — `agent_run` 원장 + 산출물 + 스케줄 등록 3축. **원장에 없다고 안 도는
   것이 아니다**(아래 §3).

---

## 1. 이미 끝나 있던 것

문서에는 잔여로 남아 있으나 실제로는 완료된 항목이다.

| 항목 | 실제 |
|---|---|
| 자율 워커 3종 실행 원장 편입 | #263 머지 |
| README 콘솔 앱 누락 | #286 머지 |
| `scripts/` lint 편입 | `package.json` 이 이미 `{src,test,scripts}` |
| **콘솔 타이포·간격 토큰** | **`Theme.swift` 에 완비.** 화면 코드의 폰트 하드코딩 0곳, 간격 1곳(`spacing: 0`) |
| `agent-command.handler.ts` 분할(488 LOC) | 196 LOC, 핸들러 22개 파일로 분리 |
| multer 3 HIGH CVE | `pnpm.overrides` 로 `^2.1.0` 고정 |
| 멀티 AI MCP 파이프라인 | `~/.claude/commands/codex-flow.md` 외 3개 존재 |
| 자동 개발 이벤트드리븐 | `~/.claude/auto-dev/` + launchd 2개 **가동 중** |
| Slack inbox text 길이 제한 | `slack-inbox.service.ts:14` `SLACK_INBOX_TEXT_MAX_BYTES = 4_000` |
| Webhook → BullMQ 전환 | `webhook/interface/webhook.controller.ts` 가 `.add(` 로 enqueue |
| be-schema formatter mrkdwn escape | `slack/format/be-schema.formatter.ts:10,32,37` `escapeSlackMrkdwn` 적용 |

**계획해 놓고 안 만든 기능은 없다.**

마지막 세 줄은 이 문서의 초안이 "코드에서 못 찾음"을 근거로 잔여에 넣었던 항목이다. PR #294
리뷰에서 실제 위치를 지적받아 확인했더니 전부 구현돼 있었다. 뒤 두 개는 소스 주석에
`V3 mid-progress audit B4 M-2` 처럼 **그 감사 항목 번호가 그대로 적혀** 있다.

교훈은 §"어떻게 판정했나" 1번과 같다 — **못 찾은 것과 없는 것은 다르다.** 한 번의 grep 이
비었다고 미구현으로 적으면 다음 사람이 이미 있는 기능을 다시 만든다.

---

## 2. 오늘 끝낸 것

| PR | 내용 |
|---|---|
| #288 | 모의투자 3-A — 장투·단타 추천과 다음 거래일 시가 체결 |
| #289 | 이 판정 착오의 교훈 기록 |
| #292 | 모의투자 3-B — 추천 성적·그림자 성적·코스피 초과수익 |

정리: worktree 20→5개, 로컬 브랜치 64→26개. 지운 worktree 의 `.ai/` 설계·검증 기록 14개
파일은 `~/worktrees/_ai-archive/` 로 백업했다(gitignore 라 레포에 없으므로 삭제하면 사라진다).

---

## 3. 분류가 틀렸던 것 — 관측 수단이 이미 다르다

[2026-08-04 로드맵 재판정](./2026-08-04-roadmap-refresh.md) §3-c 는 `DOCS_AUDIT_*` 와
`CONTRADICTION_JUDGE` 를 "실행 0건, 처분 미정"으로 분류했다. 확인해 보니 **둘 다 돌고 있고,
원장 대신 주간 하트비트로 관측하도록 이미 설계돼 있다.**

- `docs-sync-audit` — 게이트가 꺼져 있어도, 드리프트가 0건이어도 요약 1줄을 반드시 낸다
  (`docs-sync-audit.autopilot-task.ts:30-37, 76-84`)
- `knowledge-lint` L4 — `candidates`/`judged`/`abortedByQuota` 로 "판정했고 0건"과 "판정 못 함"을
  구분해 보고한다(`knowledge-lint.port.ts:35-49`). `AUTOPILOT_KNOWLEDGE_LINT_L4_ENABLED` 는
  `!== 'false'` 판정이라 **미설정이면 켜져 있다**

두 파일 모두 주석에 이유를 적어 두었다: "LLM 을 안 쓰는 구간은 agent_run 에도 없다".
**원장 편입은 중복 구현이므로 하지 않는다.**

---

## 4. 진짜 남은 것

### 확인만 하면 되는 것 (전부 소규모)

| # | 항목 | 왜 미확인인가 |
|---|---|---|
| 1 | 저녁 블로그 승인 TTL 불일치 | Slack `response_url` 30분 만료 vs preview TTL. 이후 코드가 바뀌어 재확인 필요 |
| 2 | 기능 플래그 5종 "켤 것·끌 것·지울 것" 판정 | 7/31 로드맵 항목. 실제 `.env` 값 확인이 선행 |
| 3 | 만료된 preview 의 연동 레코드 잔류 | 8/4 발견. 무응답 만료 시 `PENDING` 영구 잔류 여부 |

초안에는 여기에 세 항목(Slack inbox 길이 제한 · Webhook→BullMQ · be-schema escape)이 더
있었으나, 리뷰에서 실제 구현 위치를 지적받아 §1 로 옮겼다. **이 세 건은 잔여가 아니다.**

### 치울 것

- **원격 브랜치 119개** — 오늘 정리한 것은 로컬뿐이다
- **`p0-career-mate`·`p0-po-eval` worktree** — PR 없는 미머지 작업. 커밋 메시지가 "worktree
  정리로 유실될 뻔한 것 보존"이라 **지우기 전에 살릴지 판정이 필요**하다
- PR 없는 로컬 브랜치 16개

### 기능 후속 (설계에 이미 범위 밖으로 적힌 것)

분할·배당 정식 처리 · 자산 곡선 PNG(Slack `files:write` 스코프 필요) · DART 재무를 추천 근거에
넣기 · 뉴스 근거 · 미국 주식 · 200봉 초과 시세 수집

---

## 5. 이 문서를 어떻게 쓰나

- 잔여를 물을 때 **여기부터** 본다. 개별 계획 문서의 "남은 것" 표는 그날의 기록이다.
- 이 문서도 스냅샷이다. **날짜가 지나면 §4 를 코드로 다시 대조**하라 — §"어떻게 판정했나" 의
  3단계가 그 절차다.
- 새 결정은 이 문서를 고치지 말고 새 파일로 남긴다(레포 규칙).
