import { StudyBriefVerdict } from './study-brief.type';
import { StudyResearchKind } from './study-research.parser';

export interface StudyDeepdiveRepoModule {
  name: string;
  description: string;
}

export interface BuildStudyDeepdivePromptInput {
  kind: StudyResearchKind;
  topic: string;
  verdict: StudyBriefVerdict;
  briefMd: string;
  sourceUrls: readonly string[];
  repoModules: readonly StudyDeepdiveRepoModule[];
}

// 오늘의 공부(15분 분량 요약)를 블로그 글로 펼치는 프롬프트.
//
// 원본 조사 프롬프트(study-brief-cron.type.ts)와 **지시가 정반대**다. 그쪽은 Slack 카드에
// 담으려고 "1,200~1,800자로 압축, 읽은 것을 전부 옮기지 마라" 로 잘라냈고, 그 압축본이
// 블로그에 올리기엔 얕다는 것이 이 확장의 출발점이다. 여기서는 같은 출처를 다시 열어
// 배경·차이·한계까지 펼친다.
//
// 문체는 손대지 않는다 — 발행 라인(publish-notion-draft.usecase.ts)이 익명화·편집 뒤
// humanizeMarkdownProse 로 사용자 말투를 입힌다. 여기서 미리 흉내 내면 두 번 윤문돼 어긋난다.
//
// **구조는 여기서 정해진다.** 문장을 다듬는 것은 윤문이 하지만, 소제목을 몇 개로 나눌지와
// 한 갈래를 얼마나 길게 끌지는 이 프롬프트의 몫이고 윤문은 그것을 바꾸지 않는다.
//
// **인용체 억제(2026-09-02 추가)의 근거.** 발행본의 문장 5~13%가 「문서는 ~라고 해요」 꼴인데
// 참조 코퍼스는 0~2% 다. 한 절에 문서 인용이 네댓 번 들어간 글이 있었고(08-27 13% · 08-28 11%),
// 예시마저 출처의 것을 그대로 옮겼다(날씨 질문 · 티켓 분류). 읽는 사람에게는 그 문서를 한 번 더
// 읽는 일이 된다.
//
// 원인은 바로 위의 「출처에서 확인한 것만 쓴다」로 보인다. 사실을 단언하는 것이 위험하니 모델이
// 출처 뒤에 숨는다 — 정직성 지시가 필기체를 부르는 부작용이다. 지시를 빼지 않고, 확인하는 것과
// 확인했다고 밝히는 것을 나눠 적었다.
//
// **「안 닿으면 안 닿는다고 써라」(2026-09-02 추가)의 근거.** 5번 갈래가 「어디에 닿는지」만
// 물으면 모델은 닿는 곳을 억지로 찾는다. 실제로 2026-08-31 발행본이 그랬고, 사람이 발행 뒤
// 「그런데 내 시스템은 SDK로 모델을 부르지 않는다」 절을 통째로 새로 써 넣었다(블로그 저장소
// 커밋 「붙지 않는 전제 위에 쓴 LiteLLM 글 — 못 붙는 이유와 걷어낸 fallback 을 본문에 넣는다」).
// 제목도 「분리하기」에서 「분리할 수 있을까」로 바뀌었다.
//
// 문체 지표로는 이 결함이 잡히지 않는다 — 그 글은 어절·평균·교대율이 모두 통과했다. 수정률
// 65%(발행본 중 최고)로만 드러났고, 무엇이 문제였는지는 diff 를 열어야 보였다.
//
// **골격 예시(2026-09-02 추가)는 참조 코퍼스의 실제 소제목이다.** 규칙 문장만으로는 「틀을 매번
// 새로 고르라」가 전달되지 않는다 — 무엇이 틀인지 보여주지 않으면 모델은 아래 여섯 갈래를 그대로
// 소제목으로 옮긴다. 세 골격은 각각 사건 추적 · 단계 발전 · 선택 연쇄이고, 참조 코퍼스에서
// 소제목 8~14개짜리 글을 골라 뽑았다.
//
// 마지막 줄(「겪은 일을 쓴 것이다」)이 없으면 위험하다. 이 글은 남의 기술을 조사하는 장르라
// 겪은 사건이 없는데, 골격만 주면 모델이 사건을 지어내 그 자리를 채운다. 「지켜야 할 것」의
// 출처 규칙과 겹쳐 두 겹으로 막는다.
//
// **소제목을 문장으로 쓰라는 것과 틀을 매번 고르라는 것도 같은 실측에서 나왔다.** 발행본
// 세 편의 소제목을 죽 늘어놓으면 아래 여섯 갈래가 그대로 보인다 — 「…생기는 문제」 →
// 「…제공하는 실행 경계」 → 「…적용할 수 있는 지점」 → 「도입 전에 확인할 조건」. 갈래를
// 나열해 두었더니 그것이 곧 소제목이 됐고, 글마다 뼈대가 같아졌다. 참조 코퍼스 쪽은 글마다
// 틀이 다르다: 겪은 사건을 하나씩 파헤치거나(사건 1·2·3), 단계마다 무엇이 나아졌는지
// 보여주거나(Phase 0·1·2), 질문으로 절을 열거나.
//
// **문장형 비율 자체는 이미 참조 코퍼스와 같다** — 세어 보니 우리 43% vs 토스 40% 이고
// 나머지 세 곳은 3~18% 다(2026-09-02 실측). 처음에 「토스는 문장, 우리는 명사구」로 적었던
// 것은 소제목 몇 개를 눈으로 보고 내린 판단이라 틀렸다. 실제 차이는 문장이냐가 아니라
// **겪은 일을 쓰느냐**다 — 토스는 자기가 겪은 사건을 쓰고 이 글은 남의 기술을 조사한다.
// 위 지시는 명사구 목차를 줄이는 방향이라 그대로 두되, 없는 경험을 지어내게 만들지는
// 않는다(「지켜야 할 것」의 출처 규칙이 그 자리를 막는다).
//
// **두 층을 쓰게 한 근거(2026-09-02).** 발행본 10편 중 소제목당 분량이 목표(600자)에 든 것은
// 2026-08-26 한 편뿐인데(509자 · 소제목 18개), 그 글만 `### ` 를 쓴다. 나머지는 전부 `## `
// 한 층에 소제목 6~9개라 절당 748~1,109자다. 6,000자 글을 한 층으로만 나누면 소제목이 열 개
// 넘게 평평히 늘어서거나, 한 절이 900자를 넘거나 둘 중 하나가 된다.
//
// 왕복 변환은 이미 두 층을 지원한다 — `markdown-to-blocks.ts` 가 `### ` 를 subheading 으로
// 받고 `blocks-to-markdown.ts` 가 heading_3 을 `### ` 로 되돌린다. 이전 지시(`## ` 만)에는
// 이유 주석이 없었고, 막으려던 것은 `# `(글 제목과 겹침) 였던 것으로 보인다.
//
// 소제목당 600자는 참조 코퍼스 실측이다(`scripts/fetch-reference-corpus.ts`, 2026-09-02) —
// 한국 기술 블로그의 소제목당 분량 중앙값이 토스 622자 · 우아한형제들 538자 · 네이버 D2 386자
// 인데, 우리 발행본은 907자였다. 같은 분량을 절반의 소제목으로 끌고 가면 한 절이 두 가지
// 이야기를 품게 되고, 읽는 사람은 어디서 화제가 바뀌었는지 표지 없이 따라가야 한다.
// (하이퍼커넥트는 1,380자로 반대쪽 끝이라 중앙값 계산에서 방향만 참고했다.)
export const buildStudyDeepdivePrompt = ({
  kind,
  topic,
  verdict,
  briefMd,
  sourceUrls,
  repoModules,
}: BuildStudyDeepdivePromptInput): string =>
  [
    `아래 주제로 기술 블로그 글 한 편을 써라. 이미 15분 분량으로 압축해 둔 조사 요약이 있고, 그것을 제대로 된 글로 펼치는 작업이다.`,
    '',
    `[주제] ${topic} (${kind === 'CONCEPT' ? '개념' : '도구'})`,
    '',
    '[왜 이 주제를 골랐나]',
    ...buildVerdictLines(verdict),
    '',
    '[압축된 조사 요약 — 출발점이지 결과물이 아니다]',
    briefMd,
    '',
    '[출처 — 반드시 다시 열어 읽어라]',
    sourceUrls.length > 0
      ? sourceUrls.map((url) => `- ${url}`).join('\n')
      : '(없음 — 주제로 직접 검색해 공식 문서·공식 레포를 찾아 읽어라)',
    '필요하면 출처를 더 찾아도 된다. 공식 문서와 공식 레포를 우선한다.',
    '',
    '[글쓴이의 시스템 — 마지막 절의 재료]',
    'TypeScript·NestJS 백엔드 개발자이고, Slack 기반 LLM 멀티 에이전트 시스템을 직접 만들어 운영한다.',
    '그 시스템의 모듈 목록이다. 이 중 이번 주제와 실제로 맞닿는 것만 골라 쓴다.',
    repoModules.length > 0
      ? repoModules
          .map((module) =>
            module.description
              ? `- ${module.name}: ${module.description}`
              : `- ${module.name}`,
          )
          .join('\n')
      : '(수집 실패 — 이 경우 마지막 절은 일반적인 적용 지점으로 쓴다)',
    '',
    '[분량과 구조]',
    '본문 4,000~6,000자. 압축하지 마라 — 압축본은 이미 있고, 그것으로는 부족해서 이 글을 쓴다.',
    '소제목은 `## ` 와 `### ` 두 층을 쓴다. `# ` 는 쓰지 마라(글 제목은 따로 붙는다).',
    '큰 흐름을 `## ` 로 잡고, 그 안이 길어지면 `### ` 로 가른다. 한 층에 소제목을 늘어놓으면 목차처럼 평평해져서, 읽는 사람이 지금 어느 대목에 있는지 알 수 없다.',
    '가장 아래 층 소제목 하나가 다루는 분량은 600자 안팎이다. 900자를 넘어가면 그 안에서 이야기가 두 갈래로 갈린 것이니 나눠라.',
    '소제목은 개념 이름이 아니라 문장으로 쓴다. `~하는 방식`·`~할 수 있는 지점`처럼 명사로 맺으면 목차가 되고, 목차는 읽히지 않는다. 소제목만 죽 이어 읽어도 이야기가 흘러가게 써라.',
    '아래 여섯 갈래는 다뤄야 할 내용이지 소제목 목록이 아니다. 그대로 옮기면 글마다 뼈대가 같아져 틀에 맞춰 찍어낸 티가 난다. 틀은 주제에 맞게 매번 새로 고르고, 여섯 갈래는 그 틀 안에 녹여 넣어라.',
    '골격의 실물이다. 베끼지 말고 주제에 맞게 변형해라.',
    '  · 문제를 하나씩 파헤치는 틀 — 「지표가 달라졌다」 → 「그런데 예상하지 못한 문제들이 생겼다」 → 「사건 1. 비어 있는 캐시」 → 「사건 2. 완벽한 알리바이」 → 「그래도 다 해결되지는 않는다」',
    '  · 단계로 나아간 과정을 보여주는 틀 — 「왜 두 직군의 협업이 매끄럽지 않았을까」 → 「Phase 0: 넘기면 처음부터 다시」 → 「Phase 1: 파일 단위로 나누기」 → 「Phase 2: 약속(인터페이스)으로 나누기」 → 「협업하며 배운 것들」',
    '  · 선택의 이유를 잇는 틀 — 「이 지면에 그 광고가 적었던 이유」 → 「플레이어블 광고의 구조」 → 「MRAID 를 고른 이유」 → 「표준을 벗어나는 요구 다루기」 → 「소재를 믿지 않는 구조」',
    '위 골격들은 **글쓴이가 겪은 일**을 쓴 것이다. 이 글은 조사한 것을 쓰므로 사건을 지어내지 마라 — 그 자리에 출처에서 확인한 쟁점·세대·선택지를 놓으면 같은 흐름이 된다. 예컨대 「사건 1·2」 자리에는 그 기술이 실제로 부딪히는 문제 두세 개가, 「Phase 0·1·2」 자리에는 그 기술이 거쳐 온 세대가 들어간다.',
    '1. 어떤 문제 상황에서 이게 필요해지는지. 장면이나 상황으로 열어라 — 문서·명세를 인용하며 시작하지 마라. 인용은 둘째 갈래에서 해도 늦지 않다.',
    '2. 이게 실제로 무엇인지 — 배경, 그리고 기존 방식과 무엇이 다른지.',
    '3. 어떻게 동작하는지. 출처에서 확인한 구체적인 내용으로.',
    '4. 한계와 성숙도. 언제 쓰면 안 되는지까지.',
    '5. 글쓴이의 시스템에 대입해 본다. 닿는 곳이 있으면 위 모듈 목록의 실제 이름으로 쓰고, **안 닿으면 안 닿는다고 써라** — 무엇이 도입의 전제인데 그 전제가 이 시스템에 없는지까지. 억지로 이어 붙인 글은 사람이 코드를 열어 보는 순간 무너진다. 안 맞는다는 결론도 이 글의 결론이 될 수 있다.',
    '6. 다음에 확인할 것 한두 가지.',
    '',
    '[지켜야 할 것]',
    '- 출처에서 확인한 것만 쓴다. 확인하지 못한 수치·날짜·API 이름은 쓰지 말고, 애매하면 그 자리를 비워라.',
    '- **확인했다는 사실을 문장마다 밝히지 마라.** 「문서는 ~라고 해요」·「README 에 따르면」 을 반복하면 옮겨 적은 필기가 된다. 사실은 그냥 단언하고 출처는 글 끝 목록에 둔다. 인용 형식은 그 사실이 논쟁적이거나 버전에 따라 갈릴 때만 쓴다.',
    '- **출처의 예시를 그대로 옮기지 마라.** 「날씨 질문」·「티켓 분류」 같은 남의 예시 자리에는 위 모듈 목록에서 고른 이 시스템의 실제 경우를 넣어라. 남의 예시를 옮기면 읽는 사람은 그 문서를 한 번 더 읽는 셈이 된다.',
    '- **문단은 판단으로 열고 근거로 받쳐라.** 사실을 쌓다가 맨 끝에 한 문장으로 정리하는 순서는 필기의 순서다. 무엇을 알아냈는지 먼저 말하고, 그렇게 판단한 근거를 뒤에 붙여라.',
    '- 코드·설정·요청 예시를 최소 두 곳에 넣어라. 개념만 설명하고 실물을 보여주지 않으면 읽는 사람이 확인할 수 없다.',
    '- 그 예시는 출처에 실제로 있는 것만 옮긴다. 출처에 없으면 지어내지 말고 대신 출처가 규정한 형식·필드 이름을 그대로 인용하라.',
    '- 예시는 ``` 로 감싼 코드블록에 넣고 언어를 표시한다(```ts, ```http, ```bash 등).',
    '- 회사명·학교명·사내 시스템 이름·업무 데이터는 쓰지 마라. 이 글은 공개 저장소에 발행된다.',
    '- 요약 재탕 금지. 위 조사 요약에 이미 있는 문장을 그대로 옮기지 말고, 출처를 읽어 알아낸 것을 더해라.',
    '',
    '[출력 형식 — 문자 그대로 준수]',
    'TITLE: 글 제목 (한국어, 40자 이내). 위 5번에서 안 맞는다는 결론이 나왔으면 제목도 그것을 담아라 — 「~하기」로 단정하면 본문과 어긋난다.',
    'TAGS: 태그3~5개를 쉼표로 구분 (영문 소문자 또는 한국어 단어)',
    '---',
    '<본문 마크다운>',
  ].join('\n');

const buildVerdictLines = (verdict: StudyBriefVerdict): string[] => {
  if (verdict.kind === 'CONCEPT') {
    return [
      `- 왜 지금 필요한가: ${verdict.whyNow}`,
      `- 어디에 닿나: ${verdict.whereItLands}`,
    ];
  }
  const lines = [
    `- 무엇이 좋아지나: ${verdict.whatImproves}`,
    `- 도입 비용: ${verdict.adoptionCost}`,
  ];
  if (verdict.caution !== undefined) {
    lines.push(`- 주의: ${verdict.caution}`);
  }
  return lines;
};
