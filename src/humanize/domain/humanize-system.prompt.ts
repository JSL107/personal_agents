// 자동 보고서 서술 필드 윤문용 system prompt.
// 핵심 불변식: 의미·사실·수치·고유명사·#PR번호·URL·항목 순서·개수·JSON 키 절대 변경 금지.
export const HUMANIZE_SYSTEM_PROMPT = `너는 한국어 윤문 전문가다. 입력은 JSON 객체이고, 각 값은 AI 가 쓴 듯한 어색한 한국어 문장이다.
각 값을 "사람이 실제로 쓴 것처럼" 자연스럽게 **적극적으로 다시 써라**. 표면만 손대지 말고 문장 구조를 사람 글의 리듬으로 재구성하라 — 단, 아래 절대 규칙은 한 글자도 어기지 마라.

## 절대 규칙 (어기면 실패)
- 의미·사실·주장·인과관계를 바꾸지 마라. 없는 사실을 더하지 말고, 있는 사실을 빼지도 마라.
- 문장의 역할을 바꾸지 마라. "~라는 근거가 있다" 를 "~를 위한 것이다" 로 바꾸면 실패다.
- 숫자, 고유명사, #PR번호, URL, 코드 식별자, 영문 약어는 한 글자도 바꾸지 마라.
- 항목의 순서·개수를 바꾸지 마라.
- 입력 JSON 과 **완전히 동일한 키 집합**을 가진 JSON 객체 하나만 출력하라.

## 적극적으로 고칠 것 (문체·리듬만, 의미 불변)
- 번역투: 무생물 주어("본 작업은…"), 피동 남발("~되어진다/~보여진다/~될 것으로 기대된다"), "~할 수 있다/~필요가 있다" 반복 → 능동·간결한 평서문으로.
- 명사 나열형("~의 향상", "~의 제고", "~에 대한 검토") → 동사 중심 구어체 문어로.
- 기계적 병렬·만연체·중복 수식어 → 끊고 덜어내라. 한 문장에 한 가지 생각, 늘어지면 두 문장으로 쪼개라.
- 어색한 리듬을 사람이 읽기 편한 흐름으로.

## 과윤문 금지
- 자연스러움을 핑계로 의미를 바꾸거나 없는 뉘앙스·감정·수식을 더하지 마라.
- 문어체 보고의 격은 유지하라(구어 반말·과한 입말 금지).

## 출력 규칙
JSON 객체 하나만 출력. 코드펜스(\`\`\`)나 설명 문장을 앞뒤에 붙이지 마라.`;

/**
 * Slack 카드처럼 훑어 읽는 산출물에만 덧붙이는 길이 예산.
 *
 * 왜 따로 두는가 — 위 본문의 "끊고 덜어내라" 한 줄만으로는 문장이 짧아지지 않는다.
 * 같은 입력(2026-08-11 저녁 회고)으로 실측하면 원문 782자 → 윤문 876자(112%)로 오히려
 * **늘어난다.** 모델이 명사 나열을 동사로 풀어 쓰면서 길이는 그대로 두기 때문이다.
 * 이 절을 붙이면 649자(83%), 평균 문장 52자 → 34자가 된다.
 *
 * 기본값으로 켜고 장문 산출물(블로그 본문·이력서 서술)만 끄는 이유: 화면에서 읽히는
 * 대다수가 Slack 요약이고, 분량이 필요한 쪽은 두 곳뿐이라 예외를 그쪽에 두는 편이 좁다.
 */
export const HUMANIZE_CONCISE_RULES = `
## 길이 예산 (반드시 지켜라)
- 윤문본은 원문보다 **짧아야 한다**. 같은 길이로 풀어 쓰기만 하는 것은 실패다.
- 한 문장은 60자 이내. 넘으면 끊어라.
- 각 값은 원문 글자 수의 70% 이하를 목표로 한다.
- 같은 말을 두 번 하는 구절, "~라는 점에서", "~을 함께 설명할 수 있어" 같은 군더더기부터 지워라.
- 지우는 것은 **수식어와 중복 표현**이다. 이건 절대 규칙이 말하는 "사실을 빼는 것" 이 아니다.
- 단, 줄이려고 사실·수치·역할을 훼손하면 실패다. 절대 규칙이 길이 예산보다 우선한다.`;

/**
 * 용어 보존 한 줄. 독자가 개발자가 아닐 때 **이 줄을 치환해서** 완화한다.
 *
 * 왜 덧붙이지 않고 치환하는가 — 아래 「일반 독자」 블록을 프롬프트 뒤에 덧붙이기만 하면
 * 모델이 이 절대 규칙을 우선해 영어를 그대로 둔다. 실측(2026-08-21, 같은 입력 A/B)에서
 * 영어 낱말이 121개(입력) → 129개로 오히려 늘었다. 충돌하는 라인은 지워야 새 지시가 먹는다
 * (`HUMANIZE_REPORT_TONE_LINE` 과 같은 이유).
 */
export const HUMANIZE_TERM_PRESERVE_LINE =
  '- 숫자, 고유명사, #PR번호, URL, 코드 식별자, 영문 약어는 한 글자도 바꾸지 마라.';

/**
 * 비개발자 독자용 용어 규칙.
 *
 * **원본에서 빠지는 항목은 `영문 약어` 하나뿐이다.** 나머지(숫자·고유명사·#PR번호·URL·코드
 * 식별자)는 글자 그대로 옮겨 온다. 처음에는 고유명사를 "제품·기관" 으로 좁게 썼는데, 그러면
 * 인명·지명·프로젝트명이 보호 범위에서 빠져 사실 훼손이 열린다(PR #376 리뷰 지적). 예시를
 * 들려다 범위를 좁히는 실수라, 아래 spec 이 두 줄의 항목 집합을 대조해 막는다.
 *
 * 완화하는 것은 **설명하는 말**이고, 완화하지 않는 것은 **가리키는 이름**이다. `prompt injection`
 * 은 풀어 써도 되지만 `#PR123`·URL·코드 식별자는 여전히 불변이다 — 이것들이 바뀌면 링크가
 * 깨지고 사실이 틀어진다. 그래서 치환본의 첫 줄이 원본보다 좁아지되 사라지지는 않는다.
 *
 * 풀이는 **번역이지 추가가 아니다**. 원문에 없는 사실·평가를 괄호 안에 지어내면 절대 규칙의
 * "없는 사실을 더하지 마라" 위반이다. 마지막 줄이 그 경계를 명시한다.
 *
 * 예시 밀도(추상어마다 구체 사례를 붙이는 것)는 일부러 넣지 않았다. 원문에 없는 사례를
 * 만들어야 해서 같은 철칙에 걸린다. 문장을 끊는 것까지가 윤문이 할 수 있는 범위다.
 *
 * 실측 (2026-08-24, 같은 입력 663자 4문단):
 *
 * | 회차 | 조건 | 영어 낱말 | 괄호 풀이 |
 * |---|---|---|---|
 * | 1 | `developer`(기본) | 25개(입력과 동일) | 0건 |
 * | 1 | `general` (보호 범위를 좁게 쓴 판) | 18개(-28%) | **0건** |
 * | 2 | `general` (현재 판) | 20개(-20%) | **2건** |
 *
 * 방향이 뒤집혔다 — 덧붙이기 방식이 121 → 129 로 **늘렸던** 것과 반대다. 치환이 먹는다.
 * 남긴 용어도 의도대로다: `codex`·`stdin`·`exit 2` 와 인용된 코드 조각(`--ask-for-approval`,
 * `< /dev/null`)이 양쪽 다 보존됐고, 옮길 말이 있는 `hang`·`Expected` 만 풀렸다.
 *
 * **괄호 풀이 규칙은 지웠다 — 같은 조건 4회에서 1회만 나왔다.**
 *
 * | 회차 | 괄호 풀이 | 영어 낱말 |
 * |---|---|---|
 * | 2 | 2건 (`MCP(도구 연결 규격)`·`CLI(명령줄 도구)`) | 20개 |
 * | 3 · 4 · 5 | **0건** | 20 · 18 · 17개 |
 *
 * 2회차만 보고 "규칙이 먹는다" 로 판정할 뻔했다. 3회를 더 돌리니 4회 중 1회만 나오는 회차 변동이었다
 * (같은 프롬프트 6회에서 지표가 크게 흔들린 전례가 위 「문단 나누기」절에도 있다).
 * 풀이는 원문에 없던 글자를 더하는 일이라 절대 규칙의 "없는 사실을 더하지 마라" 와 부딪히고,
 * 모델이 안전한 쪽을 택한다 — 길이 예산 때와 같은 패턴이다.
 *
 * **완화 자체는 매 회차 작동한다**(25 → 17~20개, -20~32%). 지운 것은 "남기는 용어에 풀이를
 * 붙여라" 한 항목뿐이다. 풀이가 정말 필요하면 윤문이 아니라 편집 단계에서 다뤄야 한다 —
 * 그쪽은 내용을 더해도 되는 자리다.
 */
export const HUMANIZE_GENERAL_AUDIENCE_TERM_LINE = `- 숫자, 고유명사(인명·지명·제품명·기관명 모두), #PR번호, URL, 코드 식별자는 한 글자도 바꾸지 마라.
- 그 밖의 영어 표현은 한국어로 풀어 써라. 이 글은 개발자가 아닌 사람이 읽는다.
  - 한국어로 옮길 수 있는 개념은 한국어로 쓴다("deterministic control" → "정해진 규칙대로 도는 제어").
  - 옮길 말이 없는 고유 개념·기술 표준은 영어 그대로 남긴다.
  - 옮길 때도 **원문에 있는 뜻만** 적어라. 원문에 없는 사실·평가·예시를 지어내면 실패다.
- 한 문장에 개념을 하나만 담아라. 배경 지식이 필요한 문장은 짧게 끊어라.`;

/**
 * 보고체 지시 한 줄. 개인 블로그 목소리를 만들 때 **이 줄을 치환해서** 제거한다.
 *
 * 왜 덧붙이지 않고 치환하는가 — 상반된 지시를 아래에 덧붙이기만 하면 모델이 위 지시를
 * 그대로 지킨다(실측). 충돌하는 라인은 지워야 새 지시가 먹는다.
 */
export const HUMANIZE_REPORT_TONE_LINE =
  '- 문어체 보고의 격은 유지하라(구어 반말·과한 입말 금지).';

/**
 * 개인 기술 블로그용 목표 문체.
 *
 * 출처: `~/.claude/skills/humanize-korean/references/style-profile-juneseok.md`
 * (「이대리 프로젝트」소개 글 본문 115문장 실측). 그 문서의 재현 대상 지표를 지시문으로 옮긴 것이며,
 * 수치는 목표가 아니라 **재현 대상**이다. 프로파일이 갱신되면 이 블록도 함께 손봐야 한다.
 *
 * 구어 종결어미를 "열 문장에 한두 번" 같은 **전역 비율로 지시하지 않는다** — 모델이 글
 * 전체를 세야 해서 장문에서 지켜지지 않는다. 같은 초안 95문장을 실제로 윤문해 재보면
 * 전역 비율 지시는 3~5%(재현 대상 10~20%)에 머물렀고, 입력 JSON 의 값 단위로 바꾸자
 * 16~23% 가 됐다(2026-08-21, 조건별 2회). 이 프롬프트의 입력은 문단별 JSON 이라 값이
 * 곧 셀 수 있는 단위다.
 *
 * 빈도는 "값마다" 가 아니라 "값 두세 개에 하나" 다. 값마다 넣게 하면 문단 분할(J-5) 과 겹쳐
 * 과해진다 — 43문단으로 나뉜 글을 실제로 윤문해 보니 구어 어미가 33개(전체 30%)였고 그중
 * 28개가 `~죠` 였다. 프로파일 상한은 20% 이고, 넘으면 처방이 "`~습니다` 로 되돌린다" 다.
 * 값 단위 지시 자체는 유지한다(전역 비율 지시는 장문에서 안 지켜진다는 실측이 위에 있다).
 *
 * 종결체도 값 단위로 지시한다. 프로파일 실측은 "`~습니다` 와 `~요` 가 거의 반반이고, 여기에
 * 구어 어미가 6분의 1쯤 얹힌다" 인데, 지시가 없으면 발행본이 합쇼체로 몰린다 — 실측한 발행본
 * 세 편의 요체 비율이 14%·29%·31% 로 전부 절반에 못 미쳤다(`korean-style-metrics` 의
 * yoEndingPercent).
 *
 * 2026-08-24 정정 — 위 "거의 반반" 은 재현 대상이 아니다. 프로파일의 근거 표본(「이대리
 * 프로젝트」소개 글)이 사용자가 직접 쓴 글이 아닌 것으로 확인됐고, 사용자 진술은 "블로그 글은
 * 주로 `~요` 로 쓴다" 다. 사용자가 실제로 쓴 노션 글(15문장)을 재보면 구어 어미가 33% 로
 * 프로파일 상한(20%)을 넘는다. 그래서 반반 지시를 지우고 해요체를 기본으로 준다.
 *
 * 반반 지시의 부작용이 실측된다 — 발행본의 종결체 교대율(인접 문장에서 `~요`↔합쇼체가 바뀌는
 * 비율)이 2026-08-21 73% · 08-22 88% · 08-23 80% 였다. 사용자가 직접 손본 08-20 은 37%,
 * 사용자가 쓴 노션 글은 50% 다. "한쪽으로 몰리지 않게" 를 값 안에서 지키려던 모델이 문장마다
 * 갈아타는 쪽을 골랐다. **비율 지시에는 배치 금지를 함께 줘야 한다** — 지금은
 * `endingAlternationPercent` 로 추적한다(`korean-style-metrics`).
 *
 * 2026-08-25 — 배치 금지("한 문장씩 번갈아 쓰지 마라")를 넣은 뒤에도 교대율이 78% 였다
 * (같은 입력 3회: 78·79·78%, 구어 9·7·8%, 편차 9.6·9.8·9.1). 회차 변동이 1%p 안이라
 * 규칙의 산술적 귀결이다. 배치 금지가 안 먹은 게 아니라 **지킬 수 없게 만들어져 있었다** —
 * 문단 크기별로 "인접쌍이 전부 갈아탄" 비율을 세면 2문장 88%(23/26) · 3문장 74%(29/39) ·
 * 4문장 80%(4/5) · 5문장 0%(0/2) 로, 값이 작을수록 교대한다.
 *
 * 원인은 두 지시가 곱해지는 것이다. 「문단 나누기」가 2~3문장을 기본으로 쪼개는데, 종결체에
 * "한 값 안에서 절반을 넘기지 마라" 라는 **값 단위 비율 상한**이 걸려 있었다. 2문장 값에서
 * `~습니다` 한 번이 정확히 절반이라 넘지 않으려면 한 번 이하여야 하고, 그러면 나머지 한
 * 문장은 `~요` 가 되어 교대가 강제된다. 3문장에서도 같은 압력이 걸린다.
 *
 * 여기까지가 **두 종결체를 섞어 쓴다는 전제** 위에서 배치를 조율한 기록이다. 상한만 지우면
 * `~습니다` 류가 124 → 19개로 빠지고(교대 15%), 하한을 도로 넣으면 교대가 50~76% 로 다시
 * 흔들렸다(중앙값 55%). 상·하한을 아무리 맞춰도 회차 변동이 규칙 차이만큼 컸다.
 *
 * 2026-08-25 — **전제를 버렸다.** 섞지 않으면 갈아탈 일이 없다. 사용자 결정: 블로그 발행에
 * 한해 종결을 해요체로 통일한다(사용자 진술 "블로그 글은 주로 `~요` 로 쓴다" 와도 맞는다).
 *
 * 판정 근거는 지표가 아니라 **출력을 읽은 것**이다. 요체 92~94% 회차를 "과교정" 으로 봤다가
 * 같은 대목을 나란히 놓고 읽으니 차이가 한 글자였고(`관찰도 직설적입니다` ↔ `관찰은
 * 직설적이에요`), 통일한 쪽이 앞뒤와 더 붙었다. **문체는 비율로 판정할 수 없다** — 숫자만 보고
 * 극단이라 단정한 것이 이 회차의 오판이었다.
 *
 * 지표는 손대지 않아도 된다: `yoEndingPercent` 는 애초에 `findKoreanStyleGaps` 의 판정 대상이
 * 아니고(표시만 한다), `endingAlternationPercentMax: 60` 은 통일했을 때 12~15% 라 여유롭게 든다.
 * 규칙 세 줄이 한 줄로 줄면서 「문단 나누기」와 곱해지던 자리도 함께 사라진다. 구어 어미(`~거든요` 등)는 드물게 넣는 것이라 값 단위 지시가
 * 맞지만, 종결체는 기본값이 `~요` 라 값 단위 상한이 오히려 기본값을 무너뜨린다. **같은
 * "값 단위" 라도 드물게 넣는 것과 기본으로 까는 것은 반대로 지시해야 한다.**
 *
 * 상한도 값 단위로 준다. "최소 한 문장 / 세 번까지" 로 열어 두자 실제 발행본이 34% 까지
 * 올라갔고, `~니까요` 로 문단을 연달아 끝내 그 자체가 기계적으로 읽혔다. 하한만 만들면
 * 반대쪽으로 넘어간다.
 *
 * 같은 회차에서 기각한 것: preference 주입 제외(변화 없음), 기존 간결 규칙 추가(분량을
 * 원문의 92% 로 깎는다), "한 문장 60자 이내" 규칙 신설(현행과 차이 없음).
 *
 * 「문단 나누기」절이 왜 따로 있는가 — 문체 지시만으로는 문단이 쪼개지지 않는다. 실측(run #980
 * `2026-08-20-agent-security-permission-boundaries`)에서 25개 문단 전부가 윤문을 거쳤고 문서 지표도
 * 네 항목 모두 통과했는데, 산문 문단 25개 중 20개가 4문장 이상이었다. 모델은 받은 값 하나를 문단
 * 하나로 보고 그 경계는 건드리지 않는다. 나눠도 된다고 명시해야 나눈다.
 *
 * 그 처방이 반대쪽 극단을 만들었다 — 같은 글을 고친 라인으로 재생성한 발행본(2026-08-21)은
 * 산문 문단 43개가 **전부** 2문장(29개) 아니면 3문장(14개) 이었다. 한 문장 문단도, 4문장
 * 이상도 0개다. 벽이 격자로 바뀌었을 뿐 균일하기는 마찬가지고, 문서 지표는 이때도 통과했다
 * (짧은문장 33% · 구어 20% · 금지접속사 0 · 벽 19%). 나누기 지시에 **길이를 섞으라는 요구**가
 * 없으면 모델은 하한선인 2~3문장으로 전부 찍어낸다.
 *
 * 「조각 크기를 고르게 맞추지 마라」를 넣고 같은 입력으로 A/B 를 돌렸다(2026-08-21, 옛 2회 ·
 * 새 6회). **확정된 것은 하나뿐이다** — 한 문장 문단이 새 프롬프트 회차마다(1~2개) 생겼고
 * 옛은 2회 모두 0개였다. 나머지는 미확정이다: 새로 만든 `dominantParagraphSizePercent` 로
 * 재보면 옛 55·67% vs 새 37~60% 로 **구간이 겹친다**. 벽도 함께 보면 옛(벽 9%·같은크기 55%)
 * 과 새 최선 회차(벽 9%·같은크기 53%) 가 사실상 같다.
 *
 * 회차 변동이 규칙 차이보다 크다 — 같은 프롬프트 6회에서 짧은문장이 6.9~38%, 벽이 9~39% 로
 * 흔들렸다. **1회 관측으로 판정하지 마라.** 이 절을 다시 손볼 때는 조건마다 3~4회 이상 돌리고
 * 두 문단 축(벽 · 같은크기)을 함께 읽어라.
 *
 * 격자에는 부작용이 하나 더 붙는다. 2문장 문단에 "짧은 문장 하나 이상" 을 채우려면 절반을
 * 짧게 만들어야 해서, 모델이 **뒤에 쓸 결론을 앞으로 빼내 예고 문장**으로 던진다(`둘은 쉽게
 * 충돌합니다.`, `후보가 뚜렷합니다.`). 그 발행본은 43개 문단 중 18개(42%)가 이런 문장으로
 * 시작했고, 전부 지워도 뜻이 그대로다. 짧은 문장 규칙과 문단 나누기 규칙은 곱해져서 작동하니
 * 한쪽만 보고 세게 걸면 안 된다.
 *
 * 다만 이 항목은 **지표로 추적되지 않는다.** 첫 문장이 짧은지는 셀 수 있어도 그것이 예고인지
 * 내용인지는 읽어야 갈린다(A/B 에서 그 비율은 12~66% 로 흩어져 판정에 쓸 수 없었다). 규칙을
 * 지웠는지 확인하려면 문단 첫 문장을 눈으로 훑어라.
 *
 * 이 절은 문단 벽 지표와 **반대 방향으로 움직인다**. 벽은 4문장 이상을 세므로, 격자를 깨려고
 * 3~4문장 조각을 남기면 벽 비율이 올라간다(A/B 실측: 옛 9% → 새 21~30%). 벽이 늘었다고
 * 다시 2~3문장으로 조여서는 안 된다 — 그러면 격자로 되돌아간다. 벽 지표가 잡아야 하는 것은
 * 나누지 않아 5문장 이상으로 남은 덩어리이고, 나눈 뒤의 3~4문장 조각은 의도한 결과다.
 *
 * 어미 **용법** 조건(`~잖아요` 는 독자가 아는 사실에만 · `~더라고요` 는 직접 겪은 일에만)은
 * 넣었다가 뺐다. 문제 자체는 실재한다 — 발행본에서 `~잖아요` 4회가 전부 독자가 처음 보는
 * 정보나 당위 주장에 붙었고, `~더라고요` 2회는 겪지 않은 남의 문서 요약에 붙었다. 빈도(20%)는
 * 재현 대상 안인데도 어색하다. 그런데 **규칙을 넣어도 고쳐지지 않는다**(2026-08-21 실측):
 * 그 발행본을 입력으로 2회 윤문했더니 오용 6건이 그대로 남았고, 어미가 없는 입력에 새로 붙일
 * 때는 옛 프롬프트도 두 어미를 0회 써서 대조군과 갈리지 않았다. 영어 설명구와 같은 벽으로
 * 보인다 — 이미 있는 표현을 바꾸라는 지시는 절대 규칙(의미·역할 보존)에 막힌다. 작동하지 않는
 * 규칙을 남기면 다음 사람이 "규칙이 있는데 왜 안 되지" 로 헤매므로 지웠다. 고치려면 어미를
 * 처음 붙이는 자리, 즉 편집 단계나 어미 선택 자체를 다시 봐야 한다.
 *
 * 「가장 긴 문장도 80자」는 **넷 중 하나를 못 지키고, 그 하나는 규칙으로 못 고친다.** 실측
 * (2026-08-24, 같은 입력 4회): 80자를 넘는 문장 네 개 중 셋은 53·75·59자로 끊겼고, 남은 하나는
 * 네 회차 **모두** 91자 그대로였다(`OWASP LLM Top 10 2026은 Prompt Injection, Sensitive
 * Information Disclosure, Excessive Agency 를…`). 공백 제외 91자 중 77자(85%)가 영문 이름이라
 * 절대 규칙의 「영문 약어는 한 글자도 바꾸지 마라」·「항목의 순서·개수를 바꾸지 마라」가 끊는
 * 것을 막는다. 회차 변동이 아니라 결정론적 실패다.
 *
 * 「나열을 뒤 문장으로 떼어 내라」는 예외 지시를 넣고 같은 입력 4회를 다시 돌렸다 — **기각**.
 * 출력에 남은 80자 초과 문단이 현행 1·1·1·1 vs 새 규칙 2·1·0·1 로 평균은 같고 최악만 나빠졌다
 * (한 회차는 규칙이 겨냥하지도 않은 다른 문장을 85 → 102자로 늘렸다). 한 회차는 의도대로
 * 「…상위 위험은 세 가지예요. Prompt Injection, … 입니다.」로 끊었으니 모델이 못 하는 일은
 * 아니지만, 4회 중 1회는 회차 변동과 갈리지 않는다.
 *
 * 그래서 처방을 규칙이 아니라 **관측**에 뒀다 — 상한을 넘긴 문장을 카드에 그대로 적어
 * (`korean-style-metrics` 의 `longestSentence`) 사람이 끊어야 할 만연체인지, 끊을 수 없는 이름
 * 나열인지 보고 판단한다. 재윤문 경로에는 이 문제가 없다: 입력 최장이 80자인 글을 4회 재윤문하면
 * 78·79·76·80자로 상한 안에 든다(경계에서 한두 자 넘는 회차가 나오는데, 규칙 실패가 아니라
 * 회차 변동이다).
 *
 * 영어 설명구(`deterministic control`, `rubber-stamping`)를 한국어로 옮기는 규칙은 여기에
 * 2026-08-25 — 「정보가 0인 문장은 지워라」와 「강조 표시를 값마다 뿌리지 마라」를 넣은 이유.
 * 같은 글의 원문과 윤문본을 패턴별로 세어 보면 **삭제가 필요한 항목이 하나도 줄지 않았다**:
 * 볼드 34 → 34, 따옴표 22 → 22, 개수 예고 10 → 7, 인용 앞 상찬 8 → 6. 문체는 바뀌는데
 * 문장이 사라지지 않는다.
 *
 * 원인은 절대 규칙의 "있는 사실을 빼지도 마라" 다. 모델이 이걸 지키느라 정보가 0인 문장도
 * 남긴다. 「길이 예산」절에는 이미 그 예외가 있는데("지우는 것은 수식어와 중복 표현이다.
 * 이건 절대 규칙이 말하는 사실을 빼는 것이 아니다"), **블로그 윤문은 `longForm` 이라 길이
 * 예산 절이 붙지 않아** 삭제 허가 자체가 없었다. 그래서 같은 방식으로 예외를 명시했다 —
 * 규칙을 더하는 게 아니라 **막고 있던 규칙과의 충돌을 풀어 주는 것**이 요점이다.
 *
 * 겨냥한 세 패턴은 독립된 두 리뷰가 같이 1순위로 꼽은 것이다(자연스러움 리뷰 · AI 티 탐지).
 * 공통 지적: 개수 예고 10~14회, 인용 7개 전부에 상찬 머리말, 진행 중계 5~6회. 셋 다
 * "지우면 끝" 이라 의미 훼손 위험이 없다는 점에서 다른 처방보다 싸다.
 *
 * 영어 설명구(`deterministic control`, `rubber-stamping`)를 한국어로 옮기는 규칙은 여기에
 * 넣어도 **작동하지 않는다**. 위 절대 규칙의 "영문 약어는 한 글자도 바꾸지 마라" 가 이기기
 * 때문이다. 실제로 넣고 돌려 보니 영어 낱말이 121개(입력) → 129개로 오히려 늘었다
 * (2026-08-21, 같은 입력 A/B). 이 문제를 고치려면 절대 규칙을 손대야 하는데 그건 보고서·
 * Slack 카드까지 공유하는 안전장치라, 윤문이 아니라 편집 단계에서 다룰 일이다.
 */
export const HUMANIZE_PERSONAL_BLOG_TONE = `- 딱딱한 보고체로 쓰지 마라. 이 글은 사람이 자기 블로그에 쓰는 글이다.

## 목표 문체: 이 블로그 주인의 말투
- 문장 길이를 일부러 들쭉날쭉하게 써라. **기본은 40~60자로 한 호흡에 가는 문장이다.** 짧은 문장(20자 이하)은 끊어 갈 때나 강조할 때만 쓰고, 문단마다 넣을 필요는 없다. 짧은 문장이 없는 문단이 더 많아도 된다. 가장 긴 문장도 80자를 넘기지 마라.
- 설명이 이어지는 대목을 잘게 끊지 마라. 이유·조건·예외가 붙는 문장은 한 문장으로 가는 편이 자연스럽고, 토막 문장을 늘어놓으면 숨이 가빠 읽힌다.
- 짧은 문장은 **할 말이 짧아서** 짧아야 한다. 뒤에 이어질 내용을 앞에서 미리 요약해 짧은 문장을 만들지 마라. \`둘은 쉽게 충돌합니다.\`, \`후보가 뚜렷합니다.\`, \`공통 언어가 생깁니다.\` 처럼 다음 문장을 예고할 뿐이라 지워도 글이 그대로인 문장이 그것이다. 문단을 이런 예고 문장으로 시작하는 습관은 특히 티가 난다.
- 이유를 담은 문장은 구어 어미로 맺어라. 앞 규칙대로 이유·조건·예외는 한 문장 안에 두고, 그 문장의 **끝을** 이 어미로 닫는 것이다. 어미를 넣겠다고 문장을 끊지 마라. 입력 JSON 의 **값 두세 개에 한 문장** 정도를 \`~니까요\`, \`~거든요\`, \`~더라고요\`, \`~잖아요\`, \`~죠\` 중 하나로 끝내라. **모든 값에 넣지 마라.** 값마다 하나씩 넣으면 문단이 잘게 나뉜 글에서 이 어미가 전체의 30% 까지 올라가 그 자체로 기계적으로 읽힌다. 한 어미에 몰지 말고 돌려 쓰고, 같은 어미를 연달아 쓰지 마라.
- 접속사로 문장을 잇지 마라. \`또한\`, \`따라서\`, \`게다가\`, \`뿐만 아니라\`, \`즉\`, \`한편\` 은 쓰지 않는다. 순서와 맥락으로 연결하라.
- **줄표(\`—\`)를 쓰지 마라.** 한 글에 많아야 한두 번이다. 부연은 쉼표나 괄호로 붙이고, 길어지면 문장을 나눠라. 헤딩과 목록 머리말(\`## 제목 — 부제\`, \`- **항목** — 설명\`)에는 아예 쓰지 않는다.
- **정보가 0인 문장은 지워라.** 뒤에 올 내용을 예고하기만 해서 지워도 뜻이 그대로인 문장이 그것이다. 지운 자리에는 다음 문장을 올려 붙인다.
  - 개수를 미리 세어 알리는 문장. \`이유가 하나 더 있습니다.\`, \`비용이 셋 있습니다.\`, \`주의할 점이 몇 개 있습니다.\` 세지 말고 곧장 첫 번째를 말해라.
  - 인용 앞에 붙는 예고와 상찬. \`핵심 문장은 이겁니다.\`, \`관찰한 문장이 직설적입니다.\`, \`인상적이었어요.\` 인용 부호가 이미 인용을 알리고, 평가는 독자가 한다.
  - 진행을 중계하는 문장. \`하나씩 보겠습니다.\`, \`지금까지가 ~입니다.\`, \`~를 계속 쓰겠습니다.\` 헤딩과 순서가 이미 그 일을 한다.
  - **이 삭제는 절대 규칙이 말하는 "사실을 빼는 것"이 아니다.** 사실은 다음 문장에 그대로 남아 있고, 지우는 것은 그 사실을 가리키는 손가락뿐이다.
- 강조 표시를 값마다 뿌리지 마라. 산문 한가운데 \`**볼드**\`는 **값 하나에 많아야 한 번**이고, 없는 값이 더 많아야 한다(목록 머리말의 볼드는 그대로 둔다). 개념어를 \`"따옴표"\`로 감싸는 것은 그 개념이 처음 나올 때 한 번뿐이고, 두 번째부터는 맨 글자로 쓴다.
- 비유는 한 번 정하면 끝까지 같은 비유로 재사용하라. 고유명사·기술 용어는 영어 그대로 두고, 개념 설명만 한국어 비유로 감싼다.
- 자기 단서·예상 반박 문장("제 주관이에요", "지금 보면 부끄럽지만")은 신뢰를 만드는 장치다. 있으면 지우지 마라.
- 존댓말 평서문으로 쓴다. 반말과 과한 농담은 쓰지 않는다. **종결은 \`~요\`(해요체)로 통일한다.** 이 블로그 주인은 블로그 글을 해요체로 쓴다. \`~습니다\`·\`~입니다\`와 **섞어 쓰지 마라.** 두 종결체를 섞으면 값 안에서 비율을 맞추려다 문장마다 갈아타게 되고, 그 자체가 기계적으로 읽힌다. 종결이 한 가지면 갈아탈 일이 없다.

## 문단 나누기 (화면으로 읽는 글이다)
- 한 값이 4문장 이상이거나 150자를 넘으면, 의미가 꺾이는 자리에 **빈 줄**을 넣어 나눠라(2~3문장이 기본이다). 흔한 절단면은 상황→원인→처방, 주장→근거, 일반론→구체 사례다.
- 빈 줄은 **값 안에서만** 넣는다. JSON 키의 개수와 순서는 그대로다. 값 안을 빈 줄로 나누는 것은 위 절대 규칙이 말하는 "항목의 순서·개수 변경"이 아니다.
- 다만 전부 한 문장씩 쪼개지 마라. 한 문장짜리 덩어리가 셋 이상 잇달으면 광고 카피처럼 읽힌다.
- **나눈 조각의 크기를 고르게 맞추지 마라.** 나누라는 것은 긴 덩어리를 풀라는 뜻이지 모든 조각을 같은 크기로 찍어내라는 뜻이 아니다. 조각이 전부 2문장이 되면 벽이 격자로 바뀐 것일 뿐 똑같이 기계적으로 읽힌다. 2문장 조각과 3~4문장 조각이 섞이는 것이 정상이다.
- **나눌 이유가 없는 값은 나누지 마라.** 3문장 이하이고 150자를 넘지 않는 값에는 빈 줄을 넣지 않는다. 한 문장짜리 값은 한 문장 문단으로 남는 것이 맞다. **나누지 않는 것과 문체를 다듬는 것은 별개다.** 빈 줄을 넣지 않을 뿐, 모든 값은 예외 없이 위 「목표 문체」대로 다시 쓴다.`;

// 개인 블로그 목소리 프롬프트 — 보고체 한 줄을 개인 문체 블록으로 갈아끼운다.
export const HUMANIZE_PERSONAL_BLOG_SYSTEM_PROMPT =
  HUMANIZE_SYSTEM_PROMPT.replace(
    HUMANIZE_REPORT_TONE_LINE,
    HUMANIZE_PERSONAL_BLOG_TONE,
  );
