/**
 * 한국어 기술 블로그를 만들 때 공유하는 품질 계약.
 *
 * 채점표 전체를 모델 출력에 그대로 노출하면 글이 체크리스트처럼 굳는다. 생성 단계에서는
 * 글의 흐름과 사실 경계를 지키는 핵심 규칙만 적용하고, 발행 전 평가는
 * `docs/korean-it-tech-blog-scorecard.md`를 기준으로 별도 수행한다.
 *
 * **항목을 이름 붙여 두고 조합하는 이유(2026-09-17).** 초안 경로마다 뒤에 붙는 단계가 다르다.
 * '오늘의 공부' 경로(`study-deepdive.prompt.ts`)는 생성 뒤 `humanizeMarkdownProse` 가 문체를
 * 다시 입히므로, 문장 호흡·줄바꿈·나열 항목을 생성 단계에 또 걸면 같은 규칙이 두 번 적용된다
 * (`humanize-system.prompt.ts:449-451,461` 에 거의 같은 문장이 이미 있다). 그래서 통째로
 * 넘기지 않고 `KOREAN_BLOG_CONTENT_RULES` 만 떼어 준다.
 *
 * `KOREAN_BLOG_SCORECARD_PROMPT` 의 항목과 순서는 예전 그대로다 — 순서를 바꾸면 그것을 쓰는
 * 두 경로의 입력이 달라진다.
 *
 * **회고 경로도 같은 이중 적용을 안고 있다(미해결).** `evening-retro-publish.autopilot-task.ts:28`
 * 이 `humanizeEveningRetro` 를 부르므로 그쪽도 생성 뒤 윤문을 지난다. 이번에 함께 고치지 않은
 * 것은 '오늘의 공부' 경로의 채점표 효과를 재는 중이어서다 — 두 경로를 같이 바꾸면 수정률이
 * 달라져도 무엇 때문인지 가릴 수 없다. 측정이 끝나면 여기부터 다시 본다.
 * Hermes 릴레이 경로(`build-blog-prompt.ts`)가 윤문을 지나는지는 확인하지 않았다.
 */
const RULES = {
  // 내용·근거·전개 — 뒤 단계가 대신 채워 줄 수 없는 것들.
  scorecard:
    '한국어 기술 블로그 통합 채점표의 기준을 따른다: 문맥 흐름, 표현 방식, 글 전개, 한국어 자연스러움, 기술 근거를 모두 챙긴다.',
  opening:
    '도입부에서 독자·문제·글의 범위를 보여 주고, 구체적인 장면에서 문제→접근→결과→교훈으로 이어 간다.',
  concrete:
    '추상 개념 뒤에는 실제 상황·구조·수치 또는 재현 조건을 붙인다. 대안과 트레이드오프, 실패와 방향 전환, 적용하지 않을 조건도 숨기지 않는다.',
  sourceBoundary:
    '공식·1차 출처, 작성자의 관찰, 종합 해석을 구분한다. 출처가 말하지 않은 내용을 출처의 주장처럼 쓰지 말고, 확인하지 못한 값과 측정하지 않은 결과는 미확인 또는 미측정으로 표시한다.',
  evidence:
    '코드·표·그림·수치는 어떤 주장을 뒷받침하는지 본문에서 설명한다. 숫자에는 단위·기간·계산 기준을 붙이고, 독자가 따라 할 수 있는 최소 실행 조건과 실패·중복·재시작의 검증 범위를 적는다.',
  closing:
    '마지막에는 처음 제기한 문제에 답하고, 독자가 다음에 해볼 실험·체크리스트·후속 읽을거리를 제시한다.',
  // 문장 호흡 — 윤문 단계가 담당한다. 윤문이 없는 경로에서만 생성 단계에 건다.
  breath:
    '문장의 호흡을 무조건 짧게 만들지 않는다. 의미가 이어지는 이유·조건·예외는 한 흐름으로 쓰고, 마침표를 빠르게 반복해 토막 내지 않는다.',
  lineBreak:
    '한 문장 안에서 의미가 이어지는데 임의로 줄바꿈하지 않는다. 상황→원인, 주장→근거, 일반론→사례처럼 관점이 바뀌는 곳에서만 문장이나 문단을 나눈다.',
  enumeration:
    '비슷한 생각을 마침표만 붙여 나열하지 않는다. 앞 문장의 주어와 대상을 다음 문장이 자연스럽게 이어받게 하고, 또한·따라서·결국 같은 접속사를 습관적으로 반복하지 않는다.',
  // 수사 — 윤문은 문장의 역할을 바꾸지 않으므로(의미 불변 계약) 생성 단계에서 놓으면 글에 없다.
  // 커밋 203b07f4 가 윤문 단계에 넣어 보고 0% 로 확인한 자리다.
  sentenceRole:
    '문장이 하는 일을 바꾼다. 종결 어미를 통일해도 모든 문장이 사실 하나를 놓고 마치면 길이가 달라도 같은 박자로 읽힌다. 묻고, 앞말을 되짚고, 예상되는 반박을 먼저 꺼내고, 답을 미뤘다가 다음 문장에서 준다.',
  question:
    '독자가 당연히 되물을 자리에는 물음을 놓고 다음 문장이 그 답이 되게 쓴다. 한 절에 한 번을 넘기지 않고, 답하지 않을 물음은 쓰지 않는다.',
} as const;

/**
 * 뒤에 윤문 단계가 붙는 경로에 넘기는 몫 — 내용·근거·전개만 담는다.
 *
 * 수사 항목(`sentenceRole`·`question`)은 일부러 뺐다. 이 상수를 쓰는 '오늘의 공부' 경로에는
 * 같은 요구가 파이프라인 맥락까지 붙은 형태로 이미 있다(`study-deepdive.prompt.ts`).
 *
 * **`scorecard` 도 뺐다(리뷰 지적).** 그 문장은 「문맥 흐름, 표현 방식, 글 전개, 한국어
 * 자연스러움, 기술 근거를 모두 챙긴다」로 채점표 **전체**를 가리킨다. 문체 항목을 덜어낸 목록
 * 맨 앞에 두면 덜어낸 축을 모델이 다시 불러오므로, 분리한 의미가 없어진다. 출처는 이 상수를
 * 받는 쪽의 블록 제목이 밝힌다.
 */
export const KOREAN_BLOG_CONTENT_RULES = [
  RULES.opening,
  RULES.concrete,
  RULES.sourceBoundary,
  RULES.evidence,
  RULES.closing,
].join('\n');

export const KOREAN_BLOG_SCORECARD_PROMPT = [
  RULES.scorecard,
  RULES.opening,
  RULES.concrete,
  RULES.sourceBoundary,
  RULES.evidence,
  RULES.breath,
  RULES.lineBreak,
  RULES.sentenceRole,
  RULES.question,
  RULES.enumeration,
  RULES.closing,
].join('\n');
